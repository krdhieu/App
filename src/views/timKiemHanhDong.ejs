<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />
        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css"
        />
        <script
            defer
            src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        ></script>
        <!-- <script defer src="/src/showHideSideBar.js"></script> -->
        <link rel="stylesheet" href="/css/styles.css" />
        <title>Lịch sử hành động</title>
    </head>
    <body style="background-color: #808080">
        <%- include('navAndSidebar.ejs') -%>
        <div class="container-fluid mt-3 ">
        <div class="text-white fs-4">
            từ <span class="text-info"><%= moment(from).format('DD/MM/YYYY, h:mm:ss a');%></span> 
            đến <span class="text-info"><%= moment(to).format('DD/MM/YYYY, h:mm:ss a');%></span>
            có <span class="text-info"><%= lichSu.length %></span> hành động được thực hiện
        </div>
        <table class="table table-dark text-center">
            <thead class="thead-dark">
              <tr>
                <th scope="col">#</th>
                <th scope="col">Mã nhân viên</th>
                <th scope="col">Tên nhân viên</th>
                <th scope="col">Hành động</th>
                <th scope="col">Thời gian thực hiện</th>
              </tr>
            </thead>
            <tbody>
                <% for(let i=0;i<lichSu.length;i++){%>
              <tr>
                <th scope="row"><%=i + 1 %></th>
                <td><%=lichSu[i].manhanvien%></td>
                <td><%=lichSu[i].tennhanvien%></td>
                <td><%=lichSu[i].hanhdong%></td>
                <td><%= moment(lichSu[i].ngaygio).format('DD/MM/YYYY, h:mm:ss a');%>   </td>
              </tr>
              <%}%>
            </tbody>
          </table>
        </div>
    </body>
</html>
