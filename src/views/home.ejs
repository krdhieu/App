<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />
        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css"
        />
        <script
            defer
            src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        ></script>
        <!-- <script defer src="/src/showHideSideBar.js"></script> -->
        <link rel="stylesheet" href="/css/styles.css" />
        <title>Trang Chủ</title>
    </head>

    <body class="mycolor-gray">
        <% if(typeof(alert)!== 'undefined' && alert==='1' ){%>
        <div
            class="alert alert-warning alert-dismissible position-absolute"
            style="z-index: 1"
        >
            <button
                type="button"
                class="btn-close"
                data-bs-dismiss="alert"
            ></button>
            <strong>Warning!</strong> Bạn không được cấp quyền vào trang này
        </div>
        <%}%> <% if(typeof(alert)!== 'undefined' && alert==='5' ){%>
        <div
            class="alert alert-warning alert-dismissible position-absolute"
            style="z-index: 1"
        >
            <button
                type="button"
                class="btn-close"
                data-bs-dismiss="alert"
            ></button>
            <strong>Warning!</strong> Bạn chưa đăng ký sáng kiến trong đợt này
            hoặc sáng kiến của bạn chưa được duyệt <%}%>
        </div>
        <% if(typeof(alert)!== 'undefined' && alert==='2' ){%>
        <div
            class="alert alert-warning alert-dismissible position-absolute"
            style="z-index: 1"
        >
            <button
                type="button"
                class="btn-close"
                data-bs-dismiss="alert"
            ></button>
            <strong>Warning!</strong> Nhân viên đã tham gia vào sáng kiến
        </div>
        <%}%> <% if(typeof(alert)!== 'undefined' && alert==='3' ){%>
        <div
            class="alert alert-warning alert-dismissible position-absolute"
            style="z-index: 1"
        >
            <button
                type="button"
                class="btn-close"
                data-bs-dismiss="alert"
            ></button>
            <strong>Warning!</strong> Đăng ký thành công, chờ xét duyệt
        </div>
        <%}%> <% if(typeof(alert)!== 'undefined' && alert==='4' ){%>
        <div
            class="alert alert-warning alert-dismissible position-absolute"
            style="z-index: 1"
        >
            <button
                type="button"
                class="btn-close"
                data-bs-dismiss="alert"
            ></button>
            <strong>Warning!</strong> Chưa tới đợt đăng ký
        </div>
        <%}%> <%- include('navAndSidebar.ejs') -%>
        <%if(typeof(dotHienTai)!=='undefined' && dotHienTai.length!==0){%>

        <div class="container-fluid">
            <div class="text-center text-white">
                <div>
                    <h1>Thông báo đợt sáng kiến</h1>
                    <h2><%=dotHienTai[0].tendotsangkien%></h2>
                </div>
                <div>
                    <h4>
                        Ngày bắt đầu:
                        <%=moment(dotHienTai[0].ngaybatdau).format('MM/DD/YYYY');%>
                    </h4>
                </div>
                <div>
                    <h4>
                        Ngày kết thúc:
                        <%=moment(dotHienTai[0].ngayketthuc).format('MM/DD/YYYY');%>
                    </h4>
                </div>
                <div>
                    <h4>
                        Hạn đăng ký:
                        <%=moment(dotHienTai[0].ngaydungdangky).format('MM/DD/YYYY');%>
                    </h4>
                </div>
                <div>
                    <h4>
                        Hạn nộp báo cáo:
                        <%=moment(dotHienTai[0].hannop).format('MM/DD/YYYY');%>
                    </h4>
                </div>
            </div>
        </div>
        <%}%> <%if(typeof(message)!== 'undefined'){%>
        <div class="container-fluid">
            <div class="text-center text-white"><h2><%=message%></h2></div>
        </div>
        <%}%>
    </body>
</html>
