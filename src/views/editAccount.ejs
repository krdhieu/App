<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
    />
    <link
        rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css"
    />
    <script
        defer
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
    ></script>
    <!-- <script defer src="/src/showHideSideBar.js"></script> -->
    <link rel="stylesheet" href="/css/styles.css" />
        <title>Quản Lý Tài Khoản</title>
    </head>
    <body class="mycolor-gray">
        <%- include('navAndSidebar.ejs') -%>
        <div class="container-fluid row mt-3">
            <div class="col-12 row">
                <div class="col-3"></div>
                <form action="/edit-role" method="post" class="col-6">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input
                            type="email"
                            class="form-control"
                            id="email"
                            name="email"
                            placeholder="Enter email"
                            readonly
                            value="<%= account[0].tentaikhoan%>"
                        />
                    </div>
                    <div class="row ">
                        <div class="form-group ">
                            <label for="inputState">Quyen</label>
                            <select id="inputState" class="form-select" name="quyen">
                                <% for(let i=0;i<allQuyen.length;i++){%>
                                <option value="<%=allQuyen[i].maquyen%>"><%=allQuyen[i].tenquyen%></option>
                                <%}%>
                            </select>
                        </div>
                    </div>
                    <div>
                        <button type="submit" class="btn btn-primary col-6 btn-login">
                            Lưu
                        </button>
                    </div>
                <a class="btn btn-danger" href="/reset-password?email=<%=account[0].tentaikhoan%>">reset mật khẩu</a>
            </form>
            </div>
        </div>
    </body>
</html>
